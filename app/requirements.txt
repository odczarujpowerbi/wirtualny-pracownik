# Rdzeń — używane już dziś przez kod w tym folderze.
PyYAML>=6.0
python-dotenv>=1.0
anthropic>=0.40           # validator_visual.py, ad_copy_generator.py, mailerlite_report_analyzer.py
requests>=2.31            # mailerlite_client.py, microsoft_graph_mail_client.py (REST Graph)
psutil>=5.9               # system_health_monitor.py — RAM i lista procesów identycznie na Windows/Linux
msal>=1.26               # microsoft_graph_mail_client.py — token app-only do Microsoft Graph (wysyłka maila)

# --- Agent lokalny (wizja/OCR/okna) ---
# Moduły screenshot_capture / window_manager / ocr_extract DZIAŁAJĄ bez tych
# pakietów (degradują się do available=False z jasnym powodem). Zainstaluj je na
# DOCELOWEJ maszynie Windows, żeby agent faktycznie "widział" ekran i pracował na
# oknach. OCR wymaga też binarki Tesseract (poza pip: instalator Windows +
# pakiety językowe pol/eng) — patrz docs/instrukcja.html.
#
# mss>=9.0                   # screenshot_capture — szybki zrzut ekranu/obszaru (wieloplatformowy)
# Pillow>=10.0               # screenshot_capture (fallback ImageGrab), ocr_extract (wejście do Tesseractu)
# pytesseract>=0.3.10        # ocr_extract — twardy odczyt tekstu/liczb (wymaga binarki Tesseract w PATH)
# pygetwindow>=0.0.9         # window_manager — proste listowanie/fokus okien
# pywinauto>=0.6.8           # window_manager — pewniejszy backend UI Automation dla aplikacji desktopowych (Windows)

# Kolejne warstwy (SKRYPTY.md) — NIE instaluj, dopóki nie piszesz danego
# workera; premature dependency bloat to ten sam problem co premature scope.
# Odkomentuj/dodaj w miarę realnej potrzeby:
#
# playwright>=1.40           # meta_ads_ui_fallback.py, browser worker, renderowanie HTML maila do zrzutu — po instalacji: `playwright install chromium`
# pandas>=2.0                # data_tidy.py, report_builder.py, source_schema_watcher.py (diff schematu)
# openpyxl>=3.1              # silnik Excela dla pandas przy plikach .xlsx
# Pillow>=10.0                # screenshot_diff.py, screenshot_annotate.py
# google-api-python-client   # Google Workspace (Docs/Sheets), Search Console, Analytics
# google-auth-httplib2
# google-auth-oauthlib
# msal>=1.26                 # Microsoft Graph (SharePoint, Microsoft 365) — inny kanał niż Playwright/UI
